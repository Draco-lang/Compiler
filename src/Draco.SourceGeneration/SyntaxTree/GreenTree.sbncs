namespace {{Namespace}};

{{for node in AbstractNodes}}
    /// <summary>
    /// {{node.Documentation}}
    /// </summary>
    internal abstract class {{node.Name}}
    {
    }
{{end}}

{{for node in Nodes}}
    /// <summary>
    /// {{node.Documentation}}
    /// </summary>
    internal sealed class {{node.Name}}
    {
    }
{{end}}
